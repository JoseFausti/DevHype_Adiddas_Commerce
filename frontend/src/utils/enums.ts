export enum Role {
    USER = 'ROLE_USER',
    ADMIN = 'ROLE_ADMIN',
}

export enum Status {
  PENDING = 'PENDING',
  COMPLETED = 'COMPLETED'
}

export enum PaymentMethod {
  VISA = 'VISA',
  MASTERCARD = 'MASTERCARD'
}