export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN',
}

export enum Status {
  APPROVED = 'APPROVED',
  PENDING = 'PENDING',
  REJECTED = 'REJECTED'
}

export enum PaymentMethod {
  MASTERCARD = 'MASTERCARD'
}